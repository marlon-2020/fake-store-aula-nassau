import { MuitaletraPipe } from './muitaletra.pipe';

describe('MuitaletraPipe', () => {
  it('create an instance', () => {
    const pipe = new MuitaletraPipe();
    expect(pipe).toBeTruthy();
  });
});
